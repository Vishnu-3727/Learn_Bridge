<?xml version="1.0" encoding="utf-8"?>
<!--
  Only the captures/ subdirectory of app-private files is shared, and only so the system camera app
  can write a photographed page into it via ACTION_IMAGE_CAPTURE. Nothing else is exposed.
-->
<paths>
    <files-path
        name="captures"
        path="captures/" />
</paths>
